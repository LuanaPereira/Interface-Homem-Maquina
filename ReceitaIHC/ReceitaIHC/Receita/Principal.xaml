<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
             android:TabbedPage.ToolbarPlacement="Bottom"
             android:TabbedPage.BarItemColor="Black"
             android:TabbedPage.BarSelectedItemColor="#FF4500"
             xmlns:nav="clr-namespace:ReceitaIHC.Receita"
             x:Class="ReceitaIHC.Receita.Principal">
  <!--Pages can be added as references or inline-->
    <NavigationPage Title="HOME" BarBackgroundColor="#FF4500" IconImageSource="icon_casa.png">
        <x:Arguments>
            <nav:Home/>
        </x:Arguments>
    </NavigationPage>

    <NavigationPage Title="CATEGORIA" BarBackgroundColor="#FF4500" IconImageSource="icon_talheres.png" >
        <x:Arguments>
            <nav:Categoria/>
        </x:Arguments>
    </NavigationPage>

    <NavigationPage Title="PERFIL" BarBackgroundColor="#FF4500" IconImageSource="icon_nome.png">
        <x:Arguments>
            <nav:Perfil/>
            
        </x:Arguments>
    </NavigationPage>
</TabbedPage>